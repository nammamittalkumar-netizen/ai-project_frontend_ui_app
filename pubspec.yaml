name: security_app
description: Security Hub App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Core
  flutter_riverpod: ^2.6.1      # keep this, works fine
  http: ^1.2.1
  intl: ^0.19.0
  video_player: ^2.8.6

  # Notifications (local only — no Firebase needed yet)


  # Navigation
  go_router: ^13.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true